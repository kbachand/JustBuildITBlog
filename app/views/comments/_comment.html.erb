<% @blog_post.comments.each do |comment| %>

    <h4><%= comment.user.username %></h4>
    <p><%= comment.comment_entry %></p>
    <p><%= time_ago_in_words(comment.created_at) %></p>
    <p><%= link_to 'edit comment', edit_comment_path %></p>
  
  <% end %>
  <br>
  <br>
